dist
node_modules
.slidev
# slides.md + pages/ are owned by scripts/check-slides.mjs — prettier mangles
# Slidev's per-slide `---`/frontmatter into stray slides, so it must not touch them.
slides.md
pages
public
*.svg
*.png
.env.local
package-lock.json
# generated by kart.new — not ours to format
.devcontainer/devcontainer.drift.json
